# Use image n8nio/n8n:1.123.75 (see Dockerfile). Do not deploy n8n 2.x/latest
# for this workflow unless you also add n8nio/runners.

# --- Required before the first deploy ---
# openssl rand -hex 32
# Set once. Never rotate it or stored credentials become unreadable.
N8N_ENCRYPTION_KEY=

# n8n listens here. Railway CLI `variable set N8N_PORT=${{PORT}}` interpolates
# empty at set-time (n8n then binds port 0). Set 5678 and target the domain at 5678.
N8N_PORT=5678
N8N_PROTOCOL=https

# After you generate a public domain, set these to that host (no trailing path).
# Example: n8n-production-xxxx.up.railway.app
N8N_HOST=
WEBHOOK_URL=https://REPLACE_ME.up.railway.app/

GENERIC_TIMEZONE=Europe/Zurich
N8N_DEFAULT_BINARY_DATA_MODE=filesystem
EXECUTIONS_DATA_PRUNE=true
EXECUTIONS_DATA_MAX_AGE=168
EXECUTIONS_DATA_SAVE_ON_ERROR=all
EXECUTIONS_DATA_SAVE_ON_SUCCESS=all
N8N_SECURE_COOKIE=true
N8N_PROXY_HOPS=1

# Optional: paste these in Railway Variables if you prefer not to use the n8n
# Credentials UI for API keys. Never commit real values.
# Do NOT set N8N_LICENSE_ACTIVATION_KEY (every boot would re-hit activation).
# OPENROUTER_API_KEY=
# NOTION_API_KEY=
# N8N_WEBHOOK_SECRET=
# GEMINI_NOTES_FOLDER_ID=
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
# N8N_ACTIVATE_WORKFLOW=true
# N8N_CLEAR_LICENSE=true

# Optional: first-user owner account is created in the n8n UI on first visit.
# Keep the Railway URL private. If you still want HTTP basic auth on older images:
# N8N_BASIC_AUTH_ACTIVE=true
# N8N_BASIC_AUTH_USER=
# N8N_BASIC_AUTH_PASSWORD=

# Optional production database (recommended once the instance is stable).
# DB_TYPE=postgresdb
# DB_POSTGRESDB_HOST=${{Postgres.PGHOST}}
# DB_POSTGRESDB_PORT=${{Postgres.PGPORT}}
# DB_POSTGRESDB_DATABASE=${{Postgres.PGDATABASE}}
# DB_POSTGRESDB_USER=${{Postgres.PGUSER}}
# DB_POSTGRESDB_PASSWORD=${{Postgres.PGPASSWORD}}
